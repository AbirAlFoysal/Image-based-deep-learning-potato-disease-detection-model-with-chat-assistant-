services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tazrin-app
    env_file:
      - .env
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
